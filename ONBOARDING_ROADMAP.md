# ONBOARDING_ROADMAP — Squeezer

> Живой файл, часть паттерна «два файла вместо одного»
> (`01_vibe-coding-agent/scenarios/two-track-development.md`), только
> экспортированного сюда, в целевой проект. Обновляется сессиями,
> работающими внутри этого репозитория — не агентом-аудитором. Источник
> текущего состояния — `01_vibe-coding-agent/reports/
> 2026-08-16_Squeezer_fix-plan.md` (внешний аудит + план исправлений).
> Рядом — пустая папка `decisions/`: append-only журнал решений по
> этому проекту, тот же формат, что в `01_vibe-coding-agent`.
>
> Не путать с собственным `ROADMAP.md` проекта (продуктовая дорожная
> карта Squeezer, версии 2.9.0/4.0.0 и т.д.) — это отдельный,
> предсуществующий файл, не трогается.

## Текущее состояние

По плану исправлений от 2026-08-16 (HEAD аудита `aa2c6d6`) — всё
закрыто сессией от 2026-08-30, подробности в `decisions/decision-log.md`:

### Технические правки

- [x] CI тестировал `import PyPDF2` (`.github/workflows/ci.yml:62`) —
  заменено на `pypdf`.
- [x] Пример `config.json` в README (`README.md:426`) не совпадал
  с реальным файлом (`output_dir`, `api_base`) — синхронизировано.
- [x] Расхождение `requirements.txt`/`pyproject.toml` — синхронизированы
  (владелец: ни один манифест не главный, оба поддерживаются).

### Открытые вопросы (решает владелец проекта)

- [x] Манифест зависимостей — решено: синхронизировать оба, не выбирать
  главный.
- [x] Фактический статус CI на GitHub — проверено напрямую (`gh run
  list`): не проходил ни разу за всю историю. Причина была двойная —
  `PyPDF2` (выше) и реальный runtime-баг в
  `Testing_vector_RAG_base/gui_tester.py:414-415` (`except ... as e`
  внутри отложенной лямбды, `NameError` при реальном исключении).
  Оба исправлены, строгая CI-проверка (`flake8 --select=E9,F63,F7,F82`)
  локально даёт 0 находок.
- [x] Свой короткий `CLAUDE.md` — создан по образцу `KODA.md`.

## Объём следующей итерации

Оба кандидата, обсуждавшиеся в сессии от 2026-09-01, сделаны в этой же
сессии — подробности и обоснование в `decisions/decision-log.md`:

- [x] **Качество данных базы ФИПИ — закрыто полностью.**
  Хронология в `decisions/decision-log.md` (записи «Данные ФИПИ, шаг
  3», «шаги 1-2» и «Качество данных ФИПИ»):
  - Найден и исправлен баг в `normalize_text()` (портил инициалы
    людей при разворачивании сокращений).
  - Из живой базы в OGE-Tutor убрано 7 чанков чистого мусора с
    титульной страницы PDF (204 → 197, первый проход без API).
  - Владелец предоставил исходный PDF — экстрактор переключён с
    `pypdf` на `pdfplumber` (заметно чище на этом документе), заодно
    найден и исправлен второй баг — в `structure_text()`
    (перенос строки внутри одной фразы распознавался как отдельный
    заголовок; из-за этого кодификатор тем резался на бессмысленные
    обрывки).
  - Владелец разрешил платный вызов API — документ полностью
    перечанкован и переэмбеддён (`--no-llm`, только эмбеддинги, без
    LLM-обогащения метаданных) — 197 → 157 чанков, перенесено в
    OGE-Tutor.
  Разблокировала Фазу 2 плана мобильного приложения (см. `OGE-9-
  Tutor-GUI-Assistant/decisions/
  2026-09-01_flutter-mobile-app-concept-plan.md`) — но актуальный
  чанк-набор изменился (197 → 157), экспорт под мобильное приложение,
  если уже начат на старых 197, нужно перезапустить.
- [x] **Новый аудит-diff — сделан.** `/code-review` (10 углов,
  диапазон `c0e4c7a..HEAD`) нашёл реальную регрессию в только что
  написанном фиксе качества данных ФИПИ (сокращения в начале
  предложения переставали разворачиваться) — исправлено более точной
  эвристикой, добавлены тесты. Диапазон диффа был крошечным (со
  времени прошлого аудита в проекте почти не было изменений, кроме
  этого же фикса) — следующий такой прогон имеет смысл делать, когда
  накопится больше нового кода.

Качество данных ФИПИ закрыто полностью 2026-09-01. Следующий шаг —
не выбран, ждёт владельца: новых кандидатов для Squeezer пока не
предложено; на стороне OGE-Tutor продолжается план мобильного
приложения.
